-- Initialize Kavo UI Engine (Bypasses Rayfield's blocked network domains)
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("Universal Automation Hub", "Midnight")

-- Generate Core UI Structuring
local MainTab = Window:NewTab("Automation")
local MainSection = MainTab:NewSection("Combat Loops")

local ConfigTab = Window:NewTab("Configurations")
local ConfigSection = ConfigTab:NewSection("Farming Adjustments")

-- Safe Core Environment Variables
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local VirtualUser = game:GetService("VirtualUser")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- Exact Network Services & Remotes Structure Mapping
local RemoteServices = ReplicatedStorage:WaitForChild("RemoteServices", 5)

local CollectDropRF = RemoteServices 
    and RemoteServices:WaitForChild("DropsService", 5) 
    and RemoteServices.DropsService:WaitForChild("RF", 5) 
    and RemoteServices.DropsService.RF:WaitForChild("CollectDrop", 5)

local UseWeaponRF = RemoteServices 
    and RemoteServices:WaitForChild("CombatService", 5)
    and RemoteServices.CombatService:WaitForChild("RF", 5)
    and RemoteServices.CombatService.RF:WaitForChild("UseWeapon", 5)

-- Strict Folder Mapping (Prevents workspace flooding performance crashes)
local Camera = workspace:FindFirstChild("Camera")
local DropsFolder = (Camera and Camera:FindFirstChild("Drops")) or workspace:FindFirstChild("Drops")
local EnemiesFolder = workspace:FindFirstChild("Enemies") or workspace:FindFirstChild("Monsters")

-- Script Global State Variables
_G.AutoFarm = false
_G.AutoCollect = false
_G.FarmDistance = 5
_G.CombatStance = "Above"

-- Helper 1: Safe Teleportation Matrix
local function teleportToPosition(pos)
    pcall(function()
        local character = LocalPlayer.Character
        if character and character:FindFirstChild("HumanoidRootPart") then
            character.HumanoidRootPart.CFrame = pos
        end
    end)
end

-- Helper 2: Closest Target Algorithm
local function getClosestEnemy()
    if not EnemiesFolder then return nil end
    local closest = nil
    local shortestDistance = math.huge
    
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return nil end
    local myHrp = character.HumanoidRootPart

    pcall(function()
        for _, enemy in ipairs(EnemiesFolder:GetChildren()) do
            if enemy:IsA("Model") and enemy:FindFirstChild("HumanoidRootPart") and enemy:FindFirstChild("Humanoid") then
                if enemy.Humanoid.Health > 0 then
                    local distance = (enemy.HumanoidRootPart.Position - myHrp.Position).Magnitude
                    if distance < shortestDistance then
                        shortestDistance = distance
                        closest = enemy
                    end
                end
            end
        end
    end)
    return closest
end

-- Helper 3: Instant Drop Processor
local function processDrop(drop)
    if not _G.AutoCollect or not CollectDropRF then return end
    task.spawn(function()
        pcall(function()
            CollectDropRF:InvokeServer(drop)
        end)
    end)
end

-- Native Anti-AFK Hook
pcall(function()
    LocalPlayer.Idled:Connect(function()
        VirtualUser:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
        task.wait(1)
        VirtualUser:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
    end)
end)

-- ====================================================================
-- BUILDING UI INTERACTIONS
-- ====================================================================

-- 1. Auto Farm Toggle
MainSection:NewToggle("Auto Farm Enemies", "Teleports to and automatically attacks targets", function(state)
    _G.AutoFarm = state
    if state then
        task.spawn(function()
            while _G.AutoFarm do
                local target = getClosestEnemy()
                if target and target:FindFirstChild("HumanoidRootPart") then
                    
                    -- Corrected Pure Local CFrame Transformations
                    local targetCFrame = target.HumanoidRootPart.CFrame
                    local finalCFrame = targetCFrame * CFrame.new(0, _G.FarmDistance, 0) -- Default Above
                    
                    if _G.CombatStance == "Behind" then
                        finalCFrame = targetCFrame * CFrame.new(0, 0, _G.FarmDistance)
                    elseif _G.CombatStance == "Below" then
                        finalCFrame = targetCFrame * CFrame.new(0, -_G.FarmDistance, 0)
                    end
                    
                    teleportToPosition(finalCFrame)
                    
                    -- Remote Weapon Invocation
                    pcall(function()
                        if UseWeaponRF then
                            UseWeaponRF:InvokeServer(1, { target }, 0.4, nil, "CS_HW_V1")[cite: 3]
                        else
                            local tool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
                            if tool then tool:Activate() end
                        end
                    end)
                end
                task.wait(0.1)
            end
        end)
    end
end)

-- 2. Auto Collect Drops Toggle
MainSection:NewToggle("Auto Collect Drops", "Instantly vacuums drops across the map via server remotes", function(state)
    _G.AutoCollect = state
    if state then
        task.spawn(function()
            while _G.AutoCollect do
                pcall(function()
                    if DropsFolder then
                        for _, drop in ipairs(DropsFolder:GetChildren()) do
                            if CollectDropRF then
                                task.spawn(function()
                                    CollectDropRF:InvokeServer(drop)
                                end)
                            else
                                teleportToPosition(drop.CFrame)
                                task.wait(0.1)
                            end
                        end
                    end
                end)
                task.wait(0.4)
            end
        end)
    end
end)

-- 3. Farm Distance Adjustment Slider
ConfigSection:NewSlider("Attack Distance", "Adjusts local farming offset spacing", 15, 2, function(v)
    _G.FarmDistance = v
end)

-- 4. Combat Stance Selector Dropdown
ConfigSection:NewDropdown("Combat Position Stance", "Changes spatial tracking behavior", {"Above", "Behind", "Below"}, function(currentOption)
    _G.CombatStance = currentOption
end)

-- 5. Active Live Listener Hook Setup
pcall(function()
    if DropsFolder then
        DropsFolder.ChildAdded:Connect(processDrop)
    end
end)
